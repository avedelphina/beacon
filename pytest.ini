[pytest]
testpaths = tests
# `backend` isn't an installed package — this is what makes `import
# backend` resolve regardless of whether pytest is invoked as a console
# script or via `python -m pytest` (the latter adds cwd to sys.path on its
# own, which is why this was invisible running it that way locally).
pythonpath = .
